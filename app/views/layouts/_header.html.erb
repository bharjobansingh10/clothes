<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand">Trendz</a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        <li>
          <%= link_to 'Home', :root %>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            Categories
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <% Category.all.each do |category| %>
              <li><%= link_to category.name, category %></li>
            <% end %>
          </ul>
        </li>
      </ul>
        <ul class="nav navbar-nav">
            <l1 class="nav-item">
              <%= form_tag products_path, :method => "get", :class => "form-inline" do %>
                <%=  text_field_tag :search, params[:search], class: "form-control mr-sm-2", placeholder: "Product Name?" %>
                <%= submit_tag("Search", :name=>nil) %>
              <% end %>
            </l1>
        </ul>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <%= link_to cart_path do %>
            <span class="glyphicon glyphicon-shopping-cart shopping-cart"></span> My Cart (<%= cart.size %>)
          <% end %>
        </li>
          <% if current_user&.present? %>
              <li><a href="#">Signed in as <%= current_user.name.present? ? current_user.name : "Customer"  %></a></li>
          <li>
          <%= link_to destroy_customer_session_path, data: { "turbo-method": :delete } do %>
            Logout
          <% end %>
          </li>
          <% else %>
          <li><%= link_to 'Login', new_customer_session_path %></li>
          <li><%= link_to 'Register', new_customer_registration_path %></li>
          <% end %>
      </ul>
    </div>
  </div>
</nav>
